{{- range .Values.services }}
---
apiVersion: v1
kind: Service
metadata:
  name: "{{ .name }}-backend"
  namespace: {{ .namespace }}
spec:
  type: ExternalName
  externalName: "{{ .name }}.backend"
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP  
{{- end }}